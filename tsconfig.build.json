// Used by "build"/postinstall; excludes tests so it never needs vitest's types.
{
  "extends": "./tsconfig.json",
  "include": [
    "packages/*/index.ts",
    "packages/*/lib/**/*",
    "packages/*/bin/**/*",
    "types/**/*.d.ts"
  ]
}
